{
  "name": "wkndr",
  "version": "0.1.0",
  "private": true,
  "description": "Weather-aware weekend deck for Amsterdam — the app lives in /app (Vite + React + TS, bun); weekly content pipeline + taste engine in /app/scripts",
  "scripts": {
    "dev": "bun run --cwd app dev",
    "test": "bun run --cwd app test",
    "archive": "python3 -m http.server 8000"
  }
}
